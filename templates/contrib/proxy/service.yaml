---
apiVersion: v1
kind: Service
metadata:
  name: socks5-proxy
spec:
  type: LoadBalancer
  selector:
    name: socks5-proxy
  ports:
    - name: http
      protocol: TCP
      port: 1080
      targetPort: 1080
