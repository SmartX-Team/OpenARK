---
apiVersion: v1
kind: Secret
metadata:
  name: socks5-proxy
  labels:
    name: socks5-proxy
    kissService: "false"
    serviceType: internal
type: Opaque
stringData:
  # TODO(user): change your own username!
  username: username
  # TODO(user): change your own password!
  password: myPassword!
