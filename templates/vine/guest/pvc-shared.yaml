---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: desktop-shared
  namespace: vine-guest
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 1Ti
  storageClassName: ceph-filesystem
