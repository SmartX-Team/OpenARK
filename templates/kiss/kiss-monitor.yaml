---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: monitor
  namespace: kiss
spec:
  replicas: 1
  strategy:
    rollingUpdate:
      maxUnavailable: 1
  selector:
    matchLabels:
      name: monitor
  template:
    metadata:
      labels:
        name: monitor
        kissService: "true"
        serviceType: internal
    spec:
      securityContext:
        seccompProfile:
          type: RuntimeDefault
      serviceAccount: kiss-controller
      containers:
        - name: monitor
          image: ghcr.io/ulagbulag-village/netai-cloud:master
          imagePullPolicy: Always
          command:
            - kiss-monitor
          env:
            - name: ANSIBLE_IMAGE
              valueFrom:
                configMapKeyRef:
                  name: ansible-images
                  key: kubespray
            - name: KISS_FORCE_RESET
              valueFrom:
                configMapKeyRef:
                  name: kiss-config
                  key: force_reset
            - name: RUST_LOG
              value: INFO
          resources:
            requests:
              cpu: 30m
              memory: 20Mi
            limits:
              cpu: 100m
              memory: 100Mi
