---
- name: List all plugins
  find:
    paths:
      - /var/lib/kubelet/plugins/
    file_type: directory
    pattern: rook-ceph.*
  register: results

- name: Remove plugins
  file:
    path: "{{ item.path }}"
    state: absent
    force: true
  with_items: "{{ results.files }}"
