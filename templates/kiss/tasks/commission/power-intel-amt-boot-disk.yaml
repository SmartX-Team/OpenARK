---
- name: Provision Power Planes - Intel AMT Common
  include: power-intel-amt-common.yaml
#
# - name: Change boot order to Disk-first for next boot
#   when: intel_amt.stat.exists
#   shell: >
#     {{ bin_dir }}/ctr run --rm
#     --device /dev/mei0
#     "quay.io/ulagbulag-village/netai-cloud-mei:latest"
#     "kiss-mei" mei
#     chassis bootparam set bootflag force_disk

# - name: Change boot order to Disk-first
#   when: ipmi.stat.exists
#   shell: >
#     {{ bin_dir }}/ctr run --rm
#     --device /dev/ipmi0
#     "quay.io/ulagbulag-village/netai-cloud-mei:latest"
#     "kiss-mei" mei
#     chassis bootdev disk options=persistent,efiboot
#   ignore_errors: true
